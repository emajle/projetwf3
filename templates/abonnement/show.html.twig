{% extends 'base.html.twig' %}

{% block title %}Abonnement{% endblock %}

{% block body %}
    <h1>Abonnement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonnement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ abonnement.name }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ abonnement.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary my-2" href="{{ path('abonnement_edit', {'id': abonnement.id}) }}">Modifier</a>
    <a class="btn btn-secondary mx-2" href="{{ path('abonnement_index') }}">Revenir à la liste</a>
    {{ include('abonnement/_delete_form.html.twig') }}
{% endblock %}
