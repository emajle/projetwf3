<form method="post" action="{{ path('visite_medical_delete', {'id': visite_medical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visite_medical.id) }}">
    <button class="btn">Delete</button>
</form>
