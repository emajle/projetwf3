<form method="post" action="{{ path('vaccins_et_operation_delete', {'id': vaccins_et_operation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vaccins_et_operation.id) }}">
    <button class="btn">Delete</button>
</form>
