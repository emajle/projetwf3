{% extends 'base.html.twig' %}

{% block title %}QrCode{% endblock %}

{% block body %}
    <h1>QrCode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qr_code.id }}</td>
            </tr>
            <tr>
                <th>Image_qrc</th>
                <td>{{ qr_code.imageQrc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('qr_code_index') }}">back to list</a>

    <a href="{{ path('qr_code_edit', {'id': qr_code.id}) }}">edit</a>

    {{ include('qr_code/_delete_form.html.twig') }}
{% endblock %}
