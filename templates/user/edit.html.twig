{% extends 'base.html.twig' %}

{% block title %}Editer {{user.pseudo |capitalize}}{% endblock %}

{% block body %}
  <h1>Modifier {{user.pseudo |capitalize}}</h1>

  
  
    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
  

<div class="d-flex my-2">
  <a class="btn btn-secondary me-2" href="{{ path('user_index') }}">Revenir à la liste</a>

  {{ include('user/_delete_form.html.twig') }}
</div>

{% endblock %}
