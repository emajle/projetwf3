{% extends 'base.html.twig' %}

{% block title %}Nouvelle User{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ include('user/_form.html.twig') }}
    <button class="btn btn-info form-btn mt-2">
    <a href="{{ path('user_index') }}" style= "">Retours a la liste</a>
    </button>
{% endblock %}
