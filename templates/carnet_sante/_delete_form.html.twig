<form method="post" action="{{ path('carnet_sante_delete', {'id': carnet_sante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carnet_sante.id) }}">
  <button class="btn btn-danger">Supprimer</button>
</form>
